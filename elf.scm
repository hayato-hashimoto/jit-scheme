(define-class <elf-segment> ()
  ((type)
   (segments)
   (virtaddr)
   (memsize)
   (flags)
   (align)))

(define-class <elf-object> ()
  ((class)
   (byteorder)
   (hverseion)
   (filetype)
   (archtype)
   (fversion)
   (entry)
   (flags)
   (segments)))

(define-class <elf-program-section> ()
 ((name)
  (type)
  (flags)
  (binary)
  (link)
  (info)
  (align)))

(define-class <elf-symbol-table> ()
  ((name)
  (value)
  (size)
  (type)
  (bind)
  (othjer)
  (sect)))
