


























































(define (%add op1 op2)

(define (modR/M op1 op2)
  (logior
; Mod bits
    (cond
      ((register? ) 
      ((and (expression? op) (displacement op)))))))
  
